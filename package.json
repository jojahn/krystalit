{
  "name": "krystalit",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "npm run build && concurrently \"wds --watch --root-dir=dist --node-resolve\" \"tsc --watch\"",
    "dev": "concurrently \"npm run start\"  \"npm run tauri dev\"",
    "dev:watch": "npm run build --watch && npm run tauri dev",
    "prebuild": "rimraf dist/**",
    "build": "tsc",
    "postbuild": "ncp ./public ./dist/",
    "tauri": "tauri",
    "bundle": "rollup ./dist/*.js --file bundle.js --format iife",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jojahn",
  "license": "ISC",
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-beta.6",
    "lit": "^2.0.0-rc.3",
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-beta.9",
    "@web/dev-server": "^0.1.21",
    "concurrently": "^6.2.1",
    "copyfiles": "^2.4.1",
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
