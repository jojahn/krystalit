{
  "name": "krystalit",
  "version": "1.0.0",
  "description": "![krystalit](assets/Chrystalite.png)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "npm run build && npm run tauri dev",
    "prebuild": "rimraf dist/**",
    "build": "tsc",
    "postbuild": "cp -r public/** dist/",
    "tauri": "tauri",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "jojahn",
  "license": "ISC",
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-beta.6",
    "lit": "^2.0.0-rc.3",
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-beta.9",
    "rimraf": "^3.0.2"
  }
}
